---
project: C:\Users\Kharki\Desktop\ABPT
sources: []
created: 2026-04-01T19:33:25.374418+00:00
tags: [anchor-geometry, calibration, generation, qwen, resume]
---

# 2026-04-02 Session

## Context

### Calibration script added with resume support
**What:** Added `scripts/run_qwen_geometry_generation_calibration.py` to compute late-layer rank1/tortuosity anchor profiles, classify anchors into mature/template/flat clusters, run base vs anchor generation, and aggregate constraint deltas. The script also supports incremental resume/checkpointing via `--resume` because full CPU runs are long.
**Why:** The user prepared a calibration experiment prompt and needed a standalone script without modifying existing modules. CPU execution of full 12-case calibration on Qwen 1.5B is slow enough that checkpointed resume is useful.
**Impact:** Repo now has a standalone calibration script that compiles and smoke-runs successfully on a single case. A full CPU run was started but is too slow for a single interactive pass; use `--resume` to continue chunked execution into `archive/qwen_geometry_generation_calibration.json` and `docs/research/qwen_geometry_generation_calibration.md`.

<details>
Context
A new calibration experiment was specified in `docs/research/calibration_agent_prompt.md`. It combines one prompt forward-pass geometry profile over late layers 18..27 with a 120-token base generation and anchor-biased generation, then maps anchor groups to positive/negative keyword constraints.

Options considered
1. Only implement the script and leave long runs manual.
2. Implement the script plus resume/checkpoint support because CPU execution is long.
3. Refactor existing utilities into src modules.

Decision
Chose option 2 while keeping the instruction boundary: created only a new script in `scripts/` and did not modify existing src modules. Added optional `--case_name`, `--limit`, and `--resume` to support smoke tests and long chunked runs. Wrapped generation in `torch.inference_mode()` and enabled `low_cpu_mem_usage=True` on model load.

Tradeoffs
- The script is self-contained, but duplicates some generation/keyword-analysis logic from existing research scripts because those helpers are not exposed as reusable src utilities.
- Full CPU execution remains slow because `generate_with_anchor_bias()` recomputes full-sequence hidden states every step.

Follow-up
Best next step is to continue the full calibration run in resumable chunks, e.g. rerun the script with `--resume` until all cases are completed, then inspect cluster-level separation in the final JSON/Markdown outputs.
</details>
